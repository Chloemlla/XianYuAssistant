# 前端源码逐文件技术索引（第 2 部分）

> 本章由静态文档生成器从当前受版本控制源码提取；它描述代码事实，不代表运行时验证结果。

- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconCircleCheck.vue

- 规模：6 行；文件大小 314 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path、polyline。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconClipboard.vue

- 规模：6 行；文件大小 364 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path、rect。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconClock.vue

- 规模：6 行；文件大小 262 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、circle、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconClose.vue

- 规模：5 行；文件大小 233 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconCode.vue

- 规模：6 行；文件大小 297 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、polyline。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconCommunity.vue

- 规模：7 行；文件大小 1054 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconCookie.vue

- 规模：9 行；文件大小 459 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、circle。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconCopy.vue

- 规模：6 行；文件大小 349 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、rect、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconDocumentation.vue

- 规模：7 行；文件大小 1254 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconDownload.vue

- 规模：7 行；文件大小 341 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path、polyline、line。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconEcosystem.vue

- 规模：7 行；文件大小 1977 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconEdit.vue

- 规模：6 行；文件大小 344 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconEmpty.vue

- 规模：8 行；文件大小 348 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconFilter.vue

- 规模：5 行；文件大小 264 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、polygon。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconFire.vue

- 规模：6 行；文件大小 462 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconHelp.vue

- 规模：7 行；文件大小 346 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、circle、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconImage.vue

- 规模：7 行；文件大小 325 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、rect、circle、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconInfo.vue

- 规模：7 行；文件大小 318 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、circle、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconKey.vue

- 规模：5 行；文件大小 328 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconLink.vue

- 规模：6 行；文件大小 350 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconLog.vue

- 规模：9 行；文件大小 425 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path、line、polyline。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconMail.vue

- 规模：6 行；文件大小 283 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、rect、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconMessage.vue

- 规模：5 行；文件大小 294 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconPackage.vue

- 规模：7 行；文件大小 295 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconPlay.vue

- 规模：6 行；文件大小 300 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、circle、polygon。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconPlus.vue

- 规模：6 行；文件大小 247 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconQrCode.vue

- 规模：11 行；文件大小 584 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、rect。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconRefresh.vue

- 规模：8 行；文件大小 342 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconRobot.vue

- 规模：10 行；文件大小 481 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、rect、circle、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconRocket.vue

- 规模：8 行；文件大小 473 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconSearch.vue

- 规模：6 行；文件大小 302 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、circle、line。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconSend.vue

- 规模：6 行；文件大小 309 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、line、polygon。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconShield.vue

- 规模：5 行；文件大小 256 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconShoppingBag.vue

- 规模：7 行；文件大小 321 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconSparkle.vue
