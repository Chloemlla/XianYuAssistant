# 前端源码逐文件技术索引（第 3 部分）

> 本章由静态文档生成器从当前受版本控制源码提取；它描述代码事实，不代表运行时验证结果。


- 规模：6 行；文件大小 359 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconStop.vue

- 规模：6 行；文件大小 306 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、circle、rect。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconSupport.vue

- 规模：7 行；文件大小 288 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconSync.vue

- 规模：8 行；文件大小 342 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconText.vue

- 规模：7 行；文件大小 345 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、polyline、line。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconToggleLeft.vue

- 规模：6 行；文件大小 312 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、rect、circle。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconTooling.vue

- 规模：19 行；文件大小 913 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconTrash.vue

- 规模：9 行；文件大小 386 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconTruck.vue

- 规模：8 行；文件大小 403 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、rect、polygon、circle。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconUser.vue

- 规模：6 行；文件大小 309 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path、circle。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconUsers.vue

- 规模：8 行；文件大小 360 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、circle、path。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconWifi.vue

- 规模：8 行；文件大小 389 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、path、circle。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconWifiOff.vue

- 规模：11 行；文件大小 553 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、line、path、circle。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/icons/IconWs.vue

- 规模：5 行；文件大小 239 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、svg、text。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/ImageUploader.vue

- 规模：278 行；文件大小 5867 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：vue、@/api/image、@/utils/toast、@/components/icons/IconImage.vue、@/components/icons/IconClose.vue。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：script、Props、Emits、HTMLInputElement、template、div、img、button、IconClose、span、IconImage、style。
- Composition API：computed、ref。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/layout/AppLayout.vue

- 规模：734 行；文件大小 16298 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：vue、vue-router、./NavMenu.vue、./UpdateDialog.vue、@/api/system、@/components/icons/IconChart.vue、@/components/icons/IconAccount.vue、@/components/icons/IconWifi.vue、@/components/icons/IconShoppingBag.vue、@/components/icons/IconTruck.vue、@/components/icons/IconMessage.vue、@/components/icons/IconRobot.vue、@/components/icons/IconChat.vue、@/components/icons/IconLog.vue、@/components/icons/IconShield.vue。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：script、typeof、any、template、div、button、span、component、transition、NavMenu、aside、main、RouterView、UpdateDialog、style。
- Composition API：computed、onMounted、onUnmounted、ref。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/layout/LoginLayout.vue

- 规模：21 行；文件大小 303 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：vue-router。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：script、template、div、RouterView、style。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/layout/NavMenu.vue

- 规模：129 行；文件大小 7857 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：script、template、nav、svg、span、div、style。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/layout/UpdateDialog.vue

- 规模：470 行；文件大小 9762 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：vue、@/api/system、@/components/icons/IconClose.vue、@/components/icons/IconCheck.vue、@/components/icons/IconSparkle.vue。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：script、template、Teleport、Transition、div、IconSparkle、h2、button、IconClose、span、IconCheck、style。
- Composition API：computed、ref。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/MultiImageUploader.vue

- 规模：254 行；文件大小 5797 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：vue、@/api/image、@/utils/toast、@/components/icons/IconPlus.vue、@/components/icons/IconClose.vue。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：script、Props、template、div、img、button、IconClose、IconPlus、span、style。
- Composition API：computed、ref。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/TheWelcome.vue

- 规模：95 行；文件大小 3510 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：./WelcomeItem.vue、./icons/IconDocumentation.vue、./icons/IconTooling.vue、./icons/IconEcosystem.vue、./icons/IconCommunity.vue、./icons/IconSupport.vue。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：/__open-in-editor?file=README.md。
- 使用的业务组件：script、template、WelcomeItem、DocumentationIcon、ToolingIcon、br、code、EcosystemIcon、CommunityIcon、SupportIcon。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/components/WelcomeItem.vue

- 规模：87 行；文件大小 1414 字节。
- 职责判断：可复用界面组件或布局组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：template、div、slot、h3、style。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/constants/orderStatus.ts

- 规模：36 行；文件大小 829 字节。
- 职责判断：Vue 应用入口或基础模块。
- 模块依赖：未提取到 ES module import。
- 导出符号：OrderStatusText、getOrderStatusText。
- 后端路径：无直接请求 URL。
- 使用的业务组件：未提取到 PascalCase 子组件标签。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/main.ts

- 规模：14 行；文件大小 243 字节。
- 职责判断：Vue 应用入口或基础模块。
- 模块依赖：vue、pinia、./App.vue、./router。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：未提取到 PascalCase 子组件标签。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/router/index.ts

- 规模：113 行；文件大小 3169 字节。
- 职责判断：Vue Router 路由表与导航守卫。
- 模块依赖：vue-router、@/utils/request。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：未提取到 PascalCase 子组件标签。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/stores/counter.ts

- 规模：12 行；文件大小 306 字节。
- 职责判断：Vue 应用入口或基础模块。
- 模块依赖：vue、pinia。
- 导出符号：useCounterStore。
- 后端路径：无直接请求 URL。
- 使用的业务组件：未提取到 PascalCase 子组件标签。
- Composition API：computed、ref。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/styles/header-selectors.css

- 规模：169 行；文件大小 2919 字节。
- 职责判断：Vue 应用入口或基础模块。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：未提取到 PascalCase 子组件标签。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/types/index.ts

- 规模：108 行；文件大小 1994 字节。
- 职责判断：Vue 应用入口或基础模块。
- 模块依赖：未提取到 ES module import。
- 导出符号：ApiResponse、Account、Goods、GoodsListRequest、WebSocketStatus、Message、QRLoginSession、AutoDeliveryConfig、AutoReplyConfig、OperationRecord。
- 后端路径：无直接请求 URL。
- 使用的业务组件：未提取到 PascalCase 子组件标签。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/utils/confirm.ts

- 规模：54 行；文件大小 2542 字节。
- 职责判断：前端请求、格式化或通用工具。
- 模块依赖：未提取到 ES module import。
- 导出符号：showConfirm。
- 后端路径：无直接请求 URL。
- 使用的业务组件：void、div、button。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/utils/index.ts

- 规模：85 行；文件大小 2693 字节。
- 职责判断：前端请求、格式化或通用工具。
- 模块依赖：./toast、./confirm。
- 导出符号：showSuccess、showError、showWarning、showInfo、formatTime、formatPrice、getGoodsStatusText、getAccountStatusText、debounce、throttle。
- 后端路径：无直接请求 URL。
- 使用的业务组件：typeof。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/utils/request.ts

- 规模：111 行；文件大小 3164 字节。
- 职责判断：前端请求、格式化或通用工具。
- 模块依赖：axios、./toast、@/types。
- 导出符号：getAuthToken、setAuthToken、clearAuthToken、getAuthUsername、isLoggedIn、request。
- 后端路径：/api。
- 使用的业务组件：any。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/utils/toast.ts

- 规模：53 行；文件大小 2821 字节。
- 职责判断：前端请求、格式化或通用工具。
- 模块依赖：未提取到 ES module import。
- 导出符号：toast。
- 后端路径：无直接请求 URL。
- 使用的业务组件：svg、span。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/views/accounts/accounts.css

- 规模：385 行；文件大小 7800 字节。
- 职责判断：业务页面、页面组合逻辑或页面私有组件。
- 模块依赖：未提取到 ES module import。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：未提取到 PascalCase 子组件标签。
- Composition API：未提取到常见响应式 API 调用。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/views/accounts/components/AccountTable.vue

- 规模：691 行；文件大小 16594 字节。
- 职责判断：业务页面、页面组合逻辑或页面私有组件。
- 模块依赖：vue、@/utils、@/types、@/components/icons/IconEdit.vue、@/components/icons/IconTrash.vue、@/components/icons/IconEmpty.vue、@/components/icons/IconClock.vue、@/components/icons/IconCheck.vue、@/components/icons/IconAlert.vue。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
- 使用的业务组件：script、Props、Emits、template、div、span、component、button、IconEdit、IconTrash、table、thead、tr、th、tbody、td、style。
- Composition API：onMounted、onUnmounted、ref。
- 维护提示：修改时应同步核对类型定义、请求响应结构、加载/错误状态和路由入口。

## vue-code/src/views/accounts/components/AddAccountDialog.vue

- 规模：218 行；文件大小 4202 字节。
- 职责判断：业务页面、页面组合逻辑或页面私有组件。
- 模块依赖：vue、@/api/account、@/utils、@/types。
- 导出符号：无命名导出或由 Vue SFC 模板隐式提供。
- 后端路径：无直接请求 URL。
