# Java 源码逐文件技术索引（第 4 部分）

> 本章由静态文档生成器从当前受版本控制源码提取；它描述代码事实，不代表运行时验证结果。

- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/KamiItemQueryReqDTO.java

- 主类型：KamiItemQueryReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：13 行；文件大小 234 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/KamiItemReqDTO.java

- 主类型：KamiItemReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：16 行；文件大小 396 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data、@NotNull(message = "卡密配置ID不能为空")、@NotBlank(message = "卡密内容不能为空")。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data、jakarta.validation.constraints.NotBlank、jakarta.validation.constraints.NotNull。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/KamiItemRespDTO.java

- 主类型：KamiItemRespDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：25 行；文件大小 426 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/LoginReqDTO.java

- 主类型：LoginReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：14 行；文件大小 247 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/LoginRespDTO.java

- 主类型：LoginRespDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：14 行；文件大小 245 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/ManualAddAccountReqDTO.java

- 主类型：ManualAddAccountReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：12 行；文件大小 274 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/ManualDeliveryReqDTO.java

- 主类型：ManualDeliveryReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：10 行；文件大小 220 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/MsgContextReqDTO.java

- 主类型：MsgContextReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：25 行；文件大小 405 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/MsgDTO.java

- 主类型：MsgDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：56 行；文件大小 918 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/MsgListReqDTO.java

- 主类型：MsgListReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：40 行；文件大小 762 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/MsgListRespDTO.java

- 主类型：MsgListRespDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：38 行；文件大小 571 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/OrderDetailReqDTO.java

- 主类型：OrderDetailReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：17 行；文件大小 379 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data、@NotNull(message = "闲鱼账号ID不能为空")、@NotNull(message = "订单ID不能为空")。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data、jakarta.validation.constraints.NotNull。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/OrderDTO.java

- 主类型：OrderDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：52 行；文件大小 903 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/OrderListReqDTO.java

- 主类型：OrderListReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：29 行；文件大小 608 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/OrderListRespDTO.java

- 主类型：OrderListRespDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：19 行；文件大小 335 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/OrderQueryReqDTO.java

- 主类型：OrderQueryReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：34 行；文件大小 627 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/PutNewDataToRAGReqDTO.java

- 主类型：PutNewDataToRAGReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：14 行；文件大小 242 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/QRLoginResponse.java

- 主类型：QRLoginResponse
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：24 行；文件大小 537 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data、@NoArgsConstructor、@AllArgsConstructor。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.AllArgsConstructor、lombok.Data、lombok.NoArgsConstructor。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/QRLoginSession.java

- 主类型：QRLoginSession
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：36 行；文件大小 1036 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：isExpired()。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/QRStatusResponse.java

- 主类型：QRStatusResponse
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：17 行；文件大小 344 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/RagAutoReplyConfigReqDTO.java

- 主类型：RagAutoReplyConfigReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：14 行；文件大小 278 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/RagAutoReplyConfigRespDTO.java

- 主类型：RagAutoReplyConfigRespDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：14 行；文件大小 282 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/RefreshItemsRespDTO.java

- 主类型：RefreshItemsRespDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：15 行；文件大小 349 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/RegisterReqDTO.java

- 主类型：RegisterReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：15 行；文件大小 287 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/SalesRevenueRespDTO.java

- 主类型：SalesRevenueRespDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：11 行；文件大小 220 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/SaveSettingReqDTO.java

- 主类型：SaveSettingReqDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：15 行；文件大小 298 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
- 项目内依赖：无显式项目内 import。
- 主要外部依赖：lombok.Data。
- Web 映射注解：无。
- 持久化表线索：无直接 SQL 或表注解线索。
- 维护提示：修改该文件时应同步检查其构造器注入对象、调用方、DTO/实体字段和异常返回约定。

## src/main/java/com/feijimiao/xianyuassistant/controller/dto/SyncProgressRespDTO.java

- 主类型：SyncProgressRespDTO
- 包：com.feijimiao.xianyuassistant.controller.dto
- 规模：19 行；文件大小 523 字节。
- 职责判断：HTTP 接口的数据传输对象，定义请求或响应的序列化边界。
- 注解：@Data。
- 显式方法：未提取到常规方法签名，可能是标记接口、纯字段对象或 Lombok 数据类型。
